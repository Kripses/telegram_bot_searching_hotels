Бот по поиску отелей

Описание команд:
    /start - выводит приветствие для нового пользователя и заносит его в базу данных. В дальнейшем использовании необязательна.
    /low - ищет отели в порядке возрастания выбранного пользователем параметра.(даты проживания выбраны на 20 - 27 числа следующего месяца)
    /high - ищет отели в порядке убывания выбранного пользователем параметра.(даты проживания выбраны на 20 - 27 числа следующего месяца)
    /custom - позволяет выбрать значения выбранного параметра от и до, также позволяет выбрать дату прибытия и дату отбытия.
    /history - выводит 10 последних поисковых запросов. После можно выбрать конкретный.
    /cancel - отменяет текущую операцию и переносит на выбор меотда поиска.

Установка:
    1.Скачать проект
    2.Установить необходимые библиотеки (pip install -r requirements.txt)
    3.Запустить файл main.py